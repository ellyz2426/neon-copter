# Neon Copter VR — Build Journal

## Build #85 | PM 2026-07-02 | Genre #75: Helicopter Altitude-Control

### Round 1 (~30 min) — Complete ✅

**Concept:** Flappy Bird-style helicopter cave-flyer in VR. Player controls altitude via thrust (tap/click/trigger) against gravity, navigating through scrolling gate obstacles while collecting orbs for combo multipliers.

**TypeScript Errors Fixed (7):**
1. `MeshEntity` — removed from import (not exported from @iwsdk/core)
2. `createTransformEntity` — removed from import (not exported)
3. `browserControls: true` — removed entirely (not in WorldOptions type; `xr: { offer: 'once' }` handles dual-runtime)
4. `world.ecs.createEntity()` → `world.createEntity()` (World is fully typed, rule #3)
5. `gsm.stats.addXP()` → `gsm.addXP()` (addXP is on GameStateManager, not Stats)
6. `world.input.keyboard` → `(world.input as any).keyboard` (XRInputManager typing)
7. `world.input.xr` → `(world.input as any).xr` (same)

**Key Lesson:** The `edit` tool failed silently on all changes to this large file — had to use `sed -i` for reliable edits. The `edit` tool reported "updated" but no changes were written.

**Stats:** 1,420 LOC, 16 files, 14 PanelUI panels, 40 achievements, 8 modes, 8 skins, 5 themes

**Deploy:** https://ellyz2426.github.io/neon-copter/
